<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug builds accept a development CA only after the user installs it. -->
    <base-config>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
</network-security-config>
